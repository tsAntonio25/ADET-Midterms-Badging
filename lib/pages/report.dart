import 'package:flutter/material.dart';

class Report extends StatelessWidget {
  
  const Report({super.key});

  @override
  Widget build(BuildContext context) {
    // test
    return Scaffold(
      body: Text('Report Submission here'),
    );
  }
}
