# Daydream Co., Ltd. Intranet for Employees

A small-scale front-end employee intranet app. Project purpose only.

## Set up the project
- Clone the repository then open it with your IDE
- Get dependencies by running the following command in the terminal
  
```sh
flutter pub get
```
